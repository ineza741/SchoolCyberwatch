# School CyberWatch frontend environment
# Copy to ".env" (never committed) and adjust if needed.
# All names must start with VITE_ to reach the browser app.
VITE_API_BASE_URL=http://localhost:8080/api
